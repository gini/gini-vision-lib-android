pipeline {
    agent any
    stages {
            stage('Test: list global variables') { 
                steps { 
                    echo "global variables: ${env}"
                }
            }
        }
}
